<%= form_for(business_business_owner) do |f| %>
  <% if business_business_owner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(business_business_owner.errors.count, "error") %> prohibited this business_business_owner from being saved:</h2>

      <ul>
      <% business_business_owner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :business_id %>
    <%= f.text_field :business_id %>
  </div>

  <div class="field">
    <%= f.label :business_owner_id %>
    <%= f.text_field :business_owner_id %>
  </div>

  <div class="field">
    <%= f.label :date_from %>
    <%= f.datetime_select :date_from %>
  </div>

  <div class="field">
    <%= f.label :date_to %>
    <%= f.datetime_select :date_to %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
