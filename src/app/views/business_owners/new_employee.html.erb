<% provide(:title, "Invite") %>
<h1>Invite new employee</h1>

<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <%= form_for(@credentials, url: business_owners_invite_employee_path) do |e| %>
        <%= e.label :email %>
        <%= e.text_field :email, class: 'form-control' %>
    <% end %>
  </div>
</div>

<%= link_to 'Back to Dashboard', dashboard_path %>
