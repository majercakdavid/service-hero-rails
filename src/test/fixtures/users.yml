# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined. If you add columns to the
# model remove the '{}' from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
admin_user_one:
  role: admin_one (Administrator)
  email: admin.one@servicehero.com
  encrypted_password: <%= User.new.send(:password_digest, 'secret') %>

bo_user_one:
  role: bo_one (BusinessOwner)
  email: business.owner.one@servicehero.com
  encrypted_password: <%= User.new.send(:password_digest, 'secret') %>

employee_user_one:
  role: employee_one (Employee)
  email: employee.one@servicehero.com
  encrypted_password: <%= User.new.send(:password_digest, 'secret') %>

customer_user_one:
  role: customer_one (Customer)
  email: customer.one@servicehero.com
  encrypted_password: <%= User.new.send(:password_digest, 'secret') %>