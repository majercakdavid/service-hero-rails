# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  email: 'invite_one@servicehero.com'
  business: one
  sender: bo_user_one
  token: <%= Digest::SHA2.new(512).hexdigest([Time.now, rand].join) %>

one:
  email: 'invite_two@servicehero.com'
  business: one
  sender: bo_user_one
  token: <%= Digest::SHA2.new(512).hexdigest([Time.now, rand].join) %>
